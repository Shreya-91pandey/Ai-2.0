<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    

    <uses-sdk
        android:minSdkVersion="32"
        android:targetSdkVersion="36" />

    <!-- Network permissions for Google Gemini API direct calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Audio recording & background voice mode permissions (Claude BellModeService) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Foreground services for Hands-Free Voice Mode (Android 14+ requirement) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 14+ Screen Capture Detection (Detect screenshots & suggest instant AI analysis) -->
    <uses-permission android:name="android.permission.DETECT_SCREEN_CAPTURE" />

    <!-- Android Health Connect API (Read wellness data for personal AI health insights) -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />

    <!-- Calendar & Alarm permissions for on-device agent actions -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- File read permissions for shared documents/images -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <queries>
        <!-- Web & Browser Intent Queries -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.WEB_SEARCH" />
        </intent>

        <!-- Health Connect Provider Queries -->
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>

        <!-- On-Device Tool Discovery Protocol (Claude DISCOVER_TOOLS / EXECUTE_TOOL) -->
        <intent>
            <action android:name="com.example.guruai.tool.DISCOVER_TOOLS" />
        </intent>
        <intent>
            <action android:name="com.example.guruai.tool.EXECUTE_TOOL" />
        </intent>
    </queries>

    <application
        android:name=".GuruApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Guru AI"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.GuruAI">

        <!-- Enterprise MDM Restrictions Definition -->
        <meta-data
            android:name="android.content.APP_RESTRICTIONS"
            android:resource="@xml/app_restrictions" />

        <!-- 1. Main Launcher Activity with Jetpack Compose & 120Hz high refresh rate -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. System Assistant Overlay Activity (Decompiled Claude classes3.dex: AssistantOverlayActivity) -->
        <!-- Responds to ACTION_ASSIST (long-press power / home swipe) and ACTION_VOICE_ASSIST -->
        <activity
            android:name=".ui.assistant.AssistantOverlayActivity"
            android:exported="true"
            android:taskAffinity="com.example.guruai.assist"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VOICE_ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 3. DeepLink & Share Receiver Activity (Decompiled Claude classes3.dex: DeepLinkActivity) -->
        <!-- Accepts shared text, PDFs, images from Chrome, WhatsApp, Docs with crash-proof unparceling -->
        <activity
            android:name=".ui.deeplink.DeepLinkActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">

            <!-- Handle single shared text, PDF, docs or images -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="text/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/msword" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <!-- Handle multiple shared files -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="text/*" />
            </intent-filter>

            <!-- Handle highlighted text menu from browser / any app -->
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- 4. Voice Mode Foreground Service (Decompiled Claude classes2.dex: BellModeService) -->
        <!-- Ongoing CallStyle Notification + MediaSession for Bluetooth headset / AirPods button controls -->
        <service
            android:name=".service.BellModeService"
            android:exported="false"
            android:foregroundServiceType="microphone|mediaPlayback" />

        <!-- 5. Android Default Digital Assistant Service (VoiceInteractionService) -->
        <service
            android:name=".service.GuruVoiceInteractionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/guru_voice_interaction_service" />
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
        </service>

        <service
            android:name=".service.GuruVoiceSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />

        <!-- 6. Android Homescreen AppWidget Provider (5x2 Glance Quick Access) -->
        <receiver
            android:name=".widget.GuruAppWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/guru_appwidget_info" />
        </receiver>

    </application>
</manifest>
